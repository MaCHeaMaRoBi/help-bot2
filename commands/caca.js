const discord = require("discord.js");

module.exports.run = async (bot, Message, args) => {

     Message.channel.send(`https://samoaramata.files.wordpress.com/2011/11/love-cacat.jpg`);
    return Message.channel.send(`**I don't give a shittt!**`)

};

module.exports.help = {
  name: "caca"
};